# Local SQLite (default if unset). File is created in the project directory.
DATABASE_URL=sqlite:./audit.db

# Optional: demo session admins (comma-separated)
# RESUMA_TODO_ADMINS=alice

# Server
PORT=3000
SITE_URL=http://127.0.0.1:3000
